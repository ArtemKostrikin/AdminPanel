import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // состояние хранилища
  },
  mutations: {
    // мутации для изменения состояния
  },
  actions: {
    // действия для выполнения асинхронных операций
  },
  modules: {
    // модули, если используются
  },
});
